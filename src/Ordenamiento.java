import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

public class Ordenamiento {
    private Menu m = new Menu();

    /*public void ordenarPorNombre(String nombre) {
        Plato platoactual;
        Plato platosiguiente;
        int n = m.getListaPlatos().size();
        for (int i = 0; i < n-1; i++) {
            for (int j = 0; j < n-i-1; j++) {
                platoactual = m.getListaPlatos().get(j);
                platosiguiente = m.getListaPlatos().get(j + 1);
                if (platoactual.compareTo(platosiguiente)>0) {
                    m.getListaPlatos().set(j, platosiguiente);
                    m.getListaPlatos().set(j + 1, platoactual);
                }
            }
        }
    }*/

    //ordenarPorTiempoPreparacion: Usa ordenamiento por inserción
    //public void ordenarPorTiempoPreparacion

}
